# -----------------------------------------------------------------------------
# JetBrains IDEs (WebStorm, IntelliJ, etc.)
# -----------------------------------------------------------------------------
# 包含了项目配置、用户偏好和本地工作区设置。
# 强烈建议在公开仓库和个人项目中忽略此目录。
.idea/

# -----------------------------------------------------------------------------
# macOS
# -----------------------------------------------------------------------------
# macOS Finder和系统服务生成的元数据文件，这些文件在你本地机器上才有意义。
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# -----------------------------------------------------------------------------
# 通用/其他
# -----------------------------------------------------------------------------
# 缩略图缓存文件 (主要是Windows系统，但也建议加入)
Thumbs.db
ehthumbs.db

# 日志文件
# 忽略日志文件是一个好习惯，因为它们通常是机器特定的运行时信息。
*.log

# 编辑器临时备份文件 (例如Vim)
*~
*.swp
*.swo